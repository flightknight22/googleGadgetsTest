<?xml version="1.0" encoding="UTF-8" ?>
<Module>
  <ModulePrefs title="Fetch JSON Example"/>
  <Content type="html">
  <![CDATA[
    <div id="content_div"></div>
    <script type="text/javascript">
    
    function makeJSONRequest() {    
      var params = {};
      params[gadgets.io.RequestParameters.CONTENT_TYPE] = gadgets.io.ContentType.JSON;
      var url = "http://api.reveldigital.com/media?api_key=qJn8lmGlCIg&format=json&";
      gadgets.io.makeRequest(url, response, params);
    };
    
    function response(obj) { 
      var jsonData = obj.data;
      var html="";
      var jsonObj = JSON.parse(JSON.stringify(jsonData));
      var srcURL="no url found";
      var newestValue = 0;
      alert("6");
      for(index in jsonObj) 
      {
        if(jsonObj[index].group_id=="uVBpt_V2xSY")
        {
          if(parseInt(jsonObj[index].name)>newestValue)
          {
            alert(Date.parse(parseInt(jsonObj[index].uploaded_on)));
            var srcURL=jsonObj[index].file_url;
            var newestValue=parseInt(jsonObj[index].name);
          }
        }
      }
      html += '<img src="' + srcURL + '">';
      document.getElementById('content_div').innerHTML = html;
     };
     
     gadgets.util.registerOnLoadHandler(makeJSONRequest);
     </script>
  ]]>
  </Content>
</Module>
